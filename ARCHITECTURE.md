## ۵. مستندات فنی و معماری (`ARCHITECTURE.md`)
وجود این فایل جداگانه برای نمره محور سوم شما **الزامی** است. در این فایل تصمیمات طراحی و ساختار را تشریح می‌کنیم.

```markdown
# 🏛 مستند معماری نرم‌افزار و تصمیمات طراحی

این سند به تشریح ساختار فنی، دلایل انتخاب ابزارها و معماری سیستم مانیتورینگ میرورها می‌پردازد.

## ۱. تصمیمات کلیدی طراحی (Design Decisions)
- **زبان برنامه‌نویسی:** زبان **Python** به دلیل داشتن کتابخانه‌های استاندارد و قدرتمند در حوزه شبکه (مانند `requests`) و خوانایی بالا انتخاب شد.
- **معیار ارزیابی (Metric):** برای سنجش سرعت، از **HTTP Round-Trip Time (RTT)** استفاده شده است. سیستم با ارسال یک درخواست `HEAD` (که سبک‌تر از `GET` است و بدنه پاسخ را دانلود نمی‌کند)، پهنای باند کاربر را هدر نداده و صرفاً زمان زنده بودن و پاسخ‌دهی سرور را می‌سنجد.
- **محیط ایزوله (Isolation):** برای جلوگیری از تداخل نسخه‌ها در سیستم‌عامل‌های مختلف ارزیاب (استاد/دستیار آموزشی)، از `venv` پایتون پکیج‌شده در یک `Makefile` استفاده شده تا پروسه Build کاملاً بازتولیدپذیر باشد.

## ۲. اجزای سیستم (Component Architecture)

سیستم از ۳ لایه اصلی تشکیل شده است:

1. **User Interface / CLI (main.py):** وظیفه دریافت پارامترهای ورودی از کاربر (نوع پکیج‌منیجر و بازه زمانی) و نمایش گزارش نهایی در خروجی را دارد.
2. **Core Monitor Engine (monitor.py):** شامل کلاس `MirrorMonitor` است که منطق تجاری برنامه (ارسال درخواست شبکه، محاسبه زمان به میلی‌ثانیه، مدیریت خطاهای شبکه نظیر Timeout و مرتب‌سازی داده‌ها) را مدیریت می‌کند.
3. **Automation Layer (Makefile):** لایه‌ای در بالاترین سطح که وابستگی‌های نرم‌افزاری را بدون دخالت کاربر مدیریت و اجرا می‌کند.

## ۳. نحوه مدیریت خطاها (Fault Tolerance)
اگر میروری به طور کامل قطع باشد یا فیلتر شده باشد، متد `ping_mirror` خطای `requests.RequestException` را صید (Catch) کرده و مقدار تأخیر آن را `float('inf')` (بی‌نهایت) در نظر می‌گیرد تا این میرور خراب، به انتهای لیست گزارش منتقل شود و سیستم دچار کرش (Crash) نشود.
