# مستندات معماری و فنی (Architecture Documentation)

## معماری کلی سیستم
این سیستم بر پایه یک اسکریپت پایتون (Python Script) ماژولار طراحی شده است. معماری سیستم شامل سه بخش اصلی است:
1. **لیست پیکربندی (Configuration):** دیکشنری `MIRRORS` که شامل نام و آدرس (URL) مخازن است.
2. **موتور تست (Testing Engine):** تابع `test_mirror` که وظیفه ارسال درخواست HTTP و محاسبه زمان پاسخ (Latency) را بر عهده دارد.
3. **تولیدکننده گزارش (Report Generator):** نتایج به‌دست‌آمده را پردازش کرده و سریع‌ترین mirror را پیدا می‌کند و در نهایت در قالب فرمت استاندارد `JSON` ذخیره می‌کند.

## تصمیمات طراحی (Design Decisions)
* **زبان برنامه‌نویسی:** از پایتون استفاده شد زیرا برای اسکریپت‌نویسی شبکه و کار با JSON بسیار قدرتمند و خواناست.
* **معیار سنجش (Metric):** معیار سنجش ما در این فاز `Latency` (زمان پاسخگویی به میلی‌ثانیه) برای یک درخواست `HTTP GET` به ریشه مخزن است. هرچه این زمان کمتر باشد، مخزن سریع‌تر است.
* **اجرای ادواری (Scheduling):** به جای پیاده‌سازی یک حلقه `while` بی‌نهایت در پایتون (که منابع سیستم را هدر می‌دهد)، تصمیم گرفته شد از ابزار استاندارد سیستم‌عامل یعنی `Cron Job` لینوکس استفاده شود. (دستور `make schedule` راهنمای آن را چاپ می‌کند).
